# ChatCode Environment Configuration
# Copy this file to .env and fill in your values

# Application Secret (used for session signing)
# Generate a secure random string for production
APP_SECRET=your-super-secret-key-change-me-in-production

# Base URL for OAuth callbacks
# Set this to your domain in production (e.g., https://yourdomain.com)
BASE_URL=http://127.0.0.1:8000

# Google OAuth Configuration
# Get these from Google Cloud Console:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Google+ API
# 4. Go to Credentials > Create Credentials > OAuth 2.0 Client IDs
# 5. Set authorized redirect URIs to: http://127.0.0.1:8000/auth/google/callback
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth Configuration
# Get these from GitHub:
# 1. Go to https://github.com/settings/developers
# 2. Click "New OAuth App"
# 3. Set Authorization callback URL to: http://127.0.0.1:8000/auth/github/callback
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# Database Configuration
# For development, you can use SQLite (default) or PostgreSQL
# SQLite (default for development):
# DB_URL=sqlite:///qr.db

# PostgreSQL (recommended for production):
# Format: postgresql://username:password@host:port/database_name
# Local PostgreSQL example:
# DB_URL=postgresql://chatcode_user:chatcode_password@localhost:5432/chatcode_db

# Production PostgreSQL example:
# DB_URL=postgresql://username:password@your-postgres-host:5432/your_database

# Optional: Enable debug mode (for development only)
# DEBUG=true
